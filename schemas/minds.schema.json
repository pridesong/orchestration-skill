{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "minds.schema.json",
  "title": "Minds",
  "description": "认知层：思维参数表，按步骤调度。回答'用什么思维做'。",
  "type": "object",
  "required": ["minds"],
  "properties": {
    "minds": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "name"],
        "properties": {
          "id": { "type": "string", "pattern": "^mind-" },
          "name": { "type": "string", "minLength": 1 },
          "type": {
            "enum": ["directive", "constraint"],
            "description": "mind 注入类型（默认 directive）：directive=正向注入（crusher 类，给一条思考路径，科研步骤用）；constraint=负向约束（FORBIDDEN 类，封死错路防幻觉区滑行，日常任务主力）"
          },
          "forbidden": {
            "type": "array",
            "items": { "type": "string", "minLength": 1 },
            "description": "constraint 型 mind 的硬约束清单（注入 T3 forbidden）；directive 型忽略"
          },
          "params": {
            "type": "object",
            "description": "思维参数：自由结构 + enforce 具象化四层（fill/schema/forbidden/check）。enforce 由 executor.py t3 合并进派发，check 由 executor.py check 机械执行——mind 从散文升级为协议。"
          },
          "enforce": {
            "type": "object",
            "description": "mind 具象化四层：把认知要求映射为可机械执行的协议约束（可选，缺失=仅散文指令）",
            "properties": {
              "fill": {
                "type": "array",
                "items": { "type": "string" },
                "description": "强制产出要求（追加到 T3 fill）：如 '每条结论必须附 evidence 字段'"
              },
              "schema": {
                "type": "object",
                "description": "产物结构强化（合并进 T3 schema）：如 { evidence: {type:'string', description:'材料原文片段'} }"
              },
              "forbidden": {
                "type": "array",
                "items": { "type": "string" },
                "description": "禁止行为（追加到 T3 forbidden）：如 '禁止用路径引用充当证据'"
              },
              "check": {
                "type": "object",
                "description": "机械验证门禁（executor.py check 执行）",
                "properties": {
                  "type": { "enum": ["evidence_in_source"] },
                  "field": {
                    "type": "string",
                    "description": "产物中要验证的字段名（递归收集所有同名字段值）"
                  },
                  "source": {
                    "type": "string",
                    "description": "证据来源文件相对路径（任务目录内），evidence 值必须作为子串出现在其中"
                  }
                },
                "required": ["type", "field", "source"]
              }
            }
          },
          "apply_to": {
            "type": "array",
            "items": { "type": "string", "pattern": "^s[0-9]{3}$" },
            "description": "此思维应用的步骤 id 列表（与 steps 的 mind_ref 双向一致性由 validate.py 检查）"
          }
        }
      }
    }
  }
}
