{
  "minds": [
    {
      "id": "mind-query",
      "name": "查询思维",
      "type": "constraint",
      "role": "diagnose",
      "forbidden_density": "zero",
      "params": { "mode": "query" },
      "forbidden": ["禁止凭记忆作答——必须真实检索", "禁止编造不存在的来源"],
      "apply_to": ["generate_01"]
    },
    {
      "id": "mind-reason",
      "name": "推理思维",
      "type": "constraint",
      "role": "architect",
      "forbidden_density": "precise",
      "params": { "mode": "reason" },
      "forbidden": ["禁止跳步——每步推导必须显式", "禁止只给结论不给推导过程"],
      "apply_to": ["generate_02"]
    },
    {
      "id": "mind-write",
      "name": "心流构造思维",
      "type": "constraint",
      "role": "write",
      "forbidden_density": "precise",
      "params": { "mode": "generate" },
      "forbidden": ["禁止编造输入中不存在的事实", "禁止用散文替代结构化产出"],
      "apply_to": ["generate_03"]
    }
  ]
}
