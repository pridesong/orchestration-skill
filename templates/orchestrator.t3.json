{
  "output_template": {
    "steps.json": {
      "task_id": "",
      "version": "0.2.0",
      "fields": [
        {
          "field": "generate_01",
          "name": "",
          "module": "mod-generate",
          "inputs": {},
          "acceptance_criteria": [""],
          "status": "pending"
        },
        {
          "field": "discriminate_01_verdict",
          "name": "",
          "module": "mod-verify",
          "inputs": {},
          "routing": { "pass": "generate_02", "fail": "generate_01" },
          "status": "pending"
        }
      ]
    },
    "minds.json": {
      "minds": [
        {
          "id": "mind-",
          "name": "",
          "type": "constraint",
          "role": "execute",
          "forbidden_density": "precise",
          "params": {},
          "forbidden": [],
          "apply_to": []
        }
      ]
    }
  },
  "fill": [
    "steps.json.fields[]: 每行 = { field, name, module, inputs, next?, parallel?, routing?, acceptance_criteria?, status }。field 命名：generate_N（产出落盘即推进）或 discriminate_N_xxx（判断项值路由分支）。module 从 modules.json 库挑选",
    "generate 字段必须声明推进：串行用 next（本字段完成后 → 目标）；并行用 parallel（组内字段同一数组声明）+ next（组全部完成 → 目标）——op-table 完全由你的声明展开，executor 零推导",
    "discriminate 字段必须有 routing：判断值 → 目标字段或 stop；复杂判别用对象形态 {to, counter, limit, escalate, mind}",
    "minds.json.minds: 定义 fields 引用/覆盖的 mind；引用现有 mind 参数集（mind-write/mind-extract/mind-audit 等）而非发明"
  ],
  "rules": [
    "模块装配优先：能用模块库的模块就不自造协议——module 决定 produce（generate/discriminate）、mind 参数集、output_schema、默认 forbidden",
    "generate 字段：产出 artifacts/<field>.json，落盘 + schema 验证即推进；必须显式声明 next（串行）或 parallel+next（并行组），禁止依赖装配顺序自动推进——状态机形态由你设计，不是由字段排列顺序决定",
    "并行语义：互不依赖的字段声明同一 parallel 组（组内每个字段都写 parallel: [组内全部字段]），组内字段写在同一行（state.csv 同行填充），组全部完成（多条件 AND）才推进到 next；串行字段各占一行",
    "discriminate 字段：产出判断项，值必须命中 routing 键；routing 目标必须是存在的字段或 stop——判别式是分支点，让状态机从线性变树；复杂判别用对象形态 {to, counter, limit, escalate, mind}（审计回退机制）",
    "inputs 连线：{参数名: 来源}，来源是前序字段名或 artifacts/ 相对路径；用于注入产物内容，不决定路由",
    "链路确立：先列出任务的独立子目标（哪些产出、哪些判断），再设计状态机形态（串行链/并行组/判别分支/回退）并逐字段声明",
    "mind 匹配：generate 用 mind-write/mind-extract/mind-transform/mind-query/mind-reason/mind-fill；discriminate 用 mind-verify/mind-audit/mind-classify——由模块默认，仅在需要时 mind_ref 覆盖",
    "acceptance_criteria 可追加（模块 verify 之后），必须机械可检查",
    "capability provenance: 模块自带的 skills/mcp 来自 modules.json；若需额外能力在任务级 modules.json 添加模块，不得在 steps.json 发明能力",
    "草稿态：产出写入 <task_dir>/draft/，只做设计收敛，不进入状态机；你完成后主 agent 会机械校验（validate.py）并派独立审计，若收到 revise 反馈（校验错误或审计意见），按反馈修改 draft/ 下的文件即可，不要重写整个协议——这是同一会话内的多轮迭代",
    "mind-orchestrator (编排者思维，LLM 心理学约束)：对抗三个默认路径——① C05 早期锚定：先列出任务的独立子目标（产出/判断）再设计状态机形态（串行/并行/分支/回退），禁止跳过目标分解直接写 fields；② C03 路径锁定：模块选择与路由设计必须由任务真实依赖驱动，不是由模板熟悉度驱动；③ C02 执行启动缺失：每个字段的验收必须机械可查。自检：每个 generate 是否显式声明了 next 或 parallel？并行组字段是否都声明同一组？discriminate 是否都有 routing？可并行字段是否已声明并行组（而非串行）？"
  ],
  "schema": {
    "steps": "schemas/steps.schema.json",
    "minds": "schemas/minds.schema.json"
  },
  "data": {
    "task": "<task goal after intent condensation>",
    "constraints": ["<implicit constraints from user intent>"],
    "deliverable": "<final deliverable definition>",
    "template_ref": "<optional: existing assembly skeleton from templates/ or examples/>",
    "intel_ref": "<optional: step1 intelligence artifact path, e.g. artifacts/step1_intel.json>",
    "modules_ref": "<task_dir>/modules.json 或 skill 内置 modules/modules.json — 模块库（produce/mind/input_schema/output_schema/routing/skills/mcp）",
    "capabilities": "<task_dir>/artifacts/capabilities.json — local capability inventory (skills + mcp_servers) produced by scripts/discover.py"
  },
  "write": {
    "steps.json": "<task_dir>/draft/steps.json",
    "minds.json": "<task_dir>/draft/minds.json"
  },
  "forbidden": [
    "do not invent modules that already exist in modules.json — reuse the library",
    "do not create fields without a module",
    "do not leave a discriminate field without routing (or module default routing)",
    "do not create dependency cycles — inputs 连线与 routing 目标不得形成回路",
    "do not set status to anything other than pending",
    "do not write prose explanations outside the JSON artifacts",
    "do not skip output: every generate field materializes artifacts/<field>.json",
    "do not declare capabilities absent from capabilities.json or the module library",
    "do not reproduce a template_ref field-for-field when data.task differs — derive the assembly from the task",
    "do not write outside draft/ — 草稿态只写 draft/steps.json 与 draft/minds.json"
  ]
}
